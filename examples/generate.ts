import {Option, pipe} from 'effect'
import {
  drawTree,
  drill,
  getValue,
  leaf,
  match,
  maximumNodeDegree,
  maximumNodeHeight,
  nthChild,
  tree,
  type Tree,
} from '../index.js'
import {convertCompilerOptionsFromJson} from 'typescript'

console.log('hello')
